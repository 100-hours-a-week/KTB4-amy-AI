#### 과제
1. 개인 프로젝트에 구축한 RAG 파이프라인을 LangChain 기반으로 마이그레이션해 보세요.
2. 마이그레이션한 LangChain 기반 RAG 파이프라인을 FastAPI로 래핑하여 REST API로 배포해 보세요.
3. LangSmith로 체인 실행을 Tracing하고 Dataset 기반으로 평가해 보세요.

#### 임베딩
- 원래는 구글에서 제공하는 제미나이 임베딩을 사용 했었으나, 개발 도중 api 한도에 도달하는 경우가 많아 허깅페이스 임베딩으로 변경하였습니다
- 사용하는 허깅페이스 임베딩은 ntfloat/multilingual-e5-base 로 선택했습니다
  - 아마 한글로 적힌 문서를 만드는 경우가 대다수 이겠으나 아무리 한글로 적혔다 하더라도 전공언어를 표현하다보면 불가피하게 외국어를 사용하는 경우가 많아 다국어를 지원하는 multilingual을 선택하였습니다
  - 다만 모델 자체가 임베딩 과정에서 너무 무거워질걸 감안하여 large 보다는 base를 선택하였습니다

#### 문서 로드
- 현재 개인프로젝트가 문서를 기반으로 튜터의 역할을 해주는것이니 만큼 만약 이를 배포하였을때 가장 많이 사용할 유저층이 어디일까 생각해보았을때 대학생이라는 결론에 다다랐습니다
- 대학의 경우 학생들의 대다수가 태블릿을 사용하기에 강의교안을 pdf로 사용하거나 pdf가 없으면 pdf로 만들어서 수업을 듣는 경우가 많았습니다 그렇기에 md, html, txt, pdf 중에서 pdf 로딩을 선택하였습니다
- pdf로드 프레임워크중에서 pymupdf 를 선택한 이유는 저희가 사용하는게 vector db인점, 교재라는 특성상 테이블이 종종 나오긴 하겠으나 그게 중요한 역할을 하는 경우는 별로 없을것 같다는 점을 들어 pymuPDFLoader를 선택하였습니다
- 저번주 바닐라 rag을 설계할때 청킹 사이즈를 120으로 했었는데 확실히 잘리는 경우가 많아 이번엔 넉넉하게 300에 오버랩 60 정도로 설정하였습니다

#### db
- db의 경우 chroma를 사용하였습니다.
  - persist directory 기능을 통하여 디스크에 저장하여 꺼내 쓰는것이 가능하다는 점이 세션이 오래 유지되기 힘든 코랩이라는 특성과 잘 맞을것 같아 FAISS 대신 Chroma를 선택하였습니다
  - 원래는 그냥 저장 기능만 구현해놨었는데 중간에 새롭게 문서 로드를 할때마다 똑같은 데이터가 계속해서 적재되고 있었다는 점을 깨닫고 os.path.exists를 통하여 파일이 없을때만 생성하여 저장하도록 바꿔주었습니다.

#### langchain
- RAG 에서 마이그레이션 해온만큼 검색기능을 넣어주었습니다. 연관도 top3를 가져오도록 하였습니다.
- temperature = 0 을 주었습니다. 아마 추후 조정이 들어가야 하지 않을까 싶긴 합니다. 이번주 위클리챌린지에서 제일 중요한것은 RAG 기능이 LangSmith로 migration을 한 뒤에도 정상작동을 하느냐 였다고 생각하기 때문에 원활한 확인을 위하여 최대한 보수적으로 설정해두었습니다 (LangSmith 평가 고려).
- ChatPromptTemplate : 챗봇에는 이게 더 알맞다는 교재를 보고 선택하였습니다. 확실히 system과 human으로 나누어서 작성하니 가독성 면에서 훨씬 나은것 같습니다

#### langsmith 평가
- 이상하게 langsmith와 연결하여 읽기는 되는데 쓰기는 안되는 현상이 있었습니다
  - langsmith 와 langchain의 버전이 다른걸 발견하여 langsmith의 버전을 낮춰주니 해결되긴 하였지만 이게 근본적인 해결 방법인지는 모르겠습니다
  - 키워드 포함 여부와 llm을 이용한 평가 시스템을 넣어놓았습니다 llm 평가 시스템이 신뢰할만하다 판단되면 단일로 가도 괜찮지 않을까 싶습니다. 일단 api 한도에 도달하는 경우가 많아 불가능에 가까운 소리 같긴 합니다
  - 원래는 이 평가를 fastapi와 연동하여 답변 생성할때마다 점수를 내어 특정 점수 이상만 출력하도록 기능을 발전시키고 싶었는데 클로드가 api 무료 플랜으로는 그냥 불가능이라고 못박아서 해보지는 않았습니다
    - 대신 개발동안 rag의 신뢰도를 평가하는 시스템으로 이용하고 추후 완성시에는 제거하는 방안을 추천 받았습니다. langsmith 평가 존재의 이유나 제가 답변에 랭스미스를 달고 싶었던 이유와 일맥상통하는것 같아서 수용하였습니다

#### fastapi
- 우선 챗봇의 형태를 띄고 있고 질문에 대한 답변 정확도 파악이 중요하다 판단되어 post 메서드 하나만 해놨습니다(루트제외). 아마 이번에는 fastapi의 메서드가 하나뿐인지라 상관 없겠지만 추후 늘어날것을 감안하면 코랩에서 로컬로 옮겨가야 하나 하는 고민이 들고 있습니다
- 아직은 단순하다는 생각이 들기도 하고 코랩이라는 특성 때문에 리팩토링을 하지는 않았지만 유지보수와 같은걸 생각하면 로컬로 옮겨가는게 맞는것 같다는 생각이 자꾸 듭니다.

#### 고민되는 점
- 출력 결과가 맘에 안들다보니 프롬프트가 계속해서 늘어나는것 같습니다. 흘러가듯 본 글 이긴 했지만 프롬프트는 너무 길어도 안좋다는 글을 본적 있기에 프롬프트가 길어져도 되는것인가 걱정이 됩니다. 지금도 맘에 드는 형태는 아니지만 맘에 드는 결과물이 나오려면 꽤 긴 프롬프트와 많은 시도가 필요할것 같아 이 부분도 일시적으로 멈춰놓고 다음 단계를 진행하였습니다