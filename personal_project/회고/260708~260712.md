### 이번주 목표
- 일정 짜기 (세밀하게)
- 목표치 재점검하기
- 답변에 대한 객관적 판단 기준 마련하기
  - notebook lm 이나 클로드를 기준으로 잡기에는 기준이 너무 높고 평가시 품이 너무 많이 든다
- 핵심 기능들 구현 완료하기
- 핵심 기능 로직
  - (문서 입력 - 개념 설명 출력 - 질문 입력 - 질문 답변 출력)
  - 현재 완료 된 기능 : 질문 입력 - 질문 답변 출력 (but 퀄리티가 맘에 들지 않음)
  - 챕터 별 분할을 추후 추가할 기능으로 제외하였으니 개념 설명 기준을 어떻게 분할할지 고민해봐야할것 같음

#### 260708
TODOLIST
- 일정 짜기 : 완료, 일단 간략하게 작성 해놨음
- 답변에 대한 객관적 기준 마련하기
- 문서 입력 기능 구현
- 그래프 - corrective rag
  - vectorstore.similarity_search_with_score : chromaDB 기준 유사도(거리)를 반환하여 주는 메서드, 반환값은 document와 score의 튜플 리스트로 이루어지게 된다.
  - 현재 코렉티브랙 구조 : initialize(필요 변수 초기화) -> search(검색해서 나온 내용, 유사도 점수) -> evaluate(평가용 값 계산하기) -> 라우터 -> 재검색시 증가노드(research_quest에 변화가 들어가야 함) 후 search 통과시 learn(프롬프트에 맞춰 답변 생성) 후 종료
    - 고민중인 점 : 평가 방안을 유사도로 하는것이 맞는가? / 재검색으로 갈시 쿼리를 어떻게 변경할것인가?
    1. 평가 방안 - 명확한 평가 기준을 세운 뒤 그에 따른 평가 방법을 세우는것이 맞을것 같음, 유사도는 임시로 두는걸로
    2. 재검색시 쿼리 
       - k값 늘리기(원래는 검색양을 늘려서 빠진 내용을 보강할까 했지만 평균 기준으로 판단하는 지금에서는 유의미한 결과를 만들어낼것 같지 않음) 
       - 새롭게 랭체인을 만들어내서 질문 변경 자체를 llm 에게 맡기기 (성능은 약속되나 프롬프트 입력 방향을 감을 못잡겠음, 만약 프롬프트를 이 쿼리가 이 기준을 통과하지 못했어 질문을 다시 짜줘라고 했을때 완전히 질문의 의도를 잃어버리면? 복구 방법이 있나?)
       - 질문을 변경하지 말고 질문 그래도 웹에서 검색하기 : 기능 구현이 될까? 지금은 문서 기반 검색만 해봐서 모르겠음
    - 결론 : 문서 기반 설명 프로그램이니 만큼 웹 검색은 조심스러워야 할것 같음 그렇기에 만족스러운 답변이 나오지 않았을 경우 1. 웹 검색 여부를 묻고 2. 오케이 사인이 떨어지면 그때 웹 검색을 3. 거부 사인이 떨어지면 원래의 대체 질문 쿼리를 적용하는걸로
      - 필요 기술 : human-in-the-loop(대화 중간에 질문), 메모리 세이버(중단된 대화를 이어가기 위함), TavilySearch?(아마도 웹검색용 툴인듯...) [참고링크](https://wikidocs.net/267811) [참고링크2](https://velog.io/@looa0807/Agent-활용-문서검색과-웹검색을-활용한-Agentic-RAG)
      - 구현 과정 : 대체 질문 쿼리(원래 하려던것, 여기서 반복과 분기 잘 나눠지는지 확인) -> 인터럽트 -> 메모리 세이버 -> TavilySearch
- 260708 완료한것 : 유사도 점수 받아와서 document의 metadata에 넣고 그 유사도로 평균 내는 노드 선언, 필요한 변수 초기화용 노드, 그래프 작동 로직 재작성

#### 260709
- 오늘의 목표 : 그래프만 끝내도 선녀겠는데
- 그래프 : corrective rag
  - 로직
    - 통과 : initialize - search - evaluate - 라우터 - learn - END - 구현 완료
    - 통과 x 인터넷 검색 x : initialize - search - evaluate - router - research - (이후 평가로 돌아감) - 구현 완료
    - 통과 x 인터넷 검색 o : initialize - search - evaluate - router - internet search - (이후 평가로 돌아감)
    - 이렇게 될 경우 실패시 같은걸 또 묻는 케이스가 발생할 수 있으니 상태확인용 변수 하나 만들어서 1일때는 웹 검색 0일때는 자체 재검색 이런식으로 가는게 좋을듯
    - 인터럽트 해서 질문하는거 잊지 말기
  - 판단 
    - 역시 AI 한테 맡기는게 나은가? 그렇다 해도 나만의 평가기준이 필요한건 사실
    - 역시 리퀘스트 쿼리 질문 재작성은 llm 한테 다시 부탁하는게 맞을듯 해보고 호출량 넘 많으면 그때가서 수정
- 오늘 회고
  - 유사도 검색 시 유사도가 거리임을 잊지 말자 (부등호 방향 신경 쓰기)
  - 내가 노드에서 반환하는것이 정확히 어디에 들어가는지 알고 잊기, state에 들어간다는걸 알고 있으면서도 자꾸 까먹음, 또 마무리는 answer로 나야지만 랭그래프가 오류 없이 돌아간다 (내가 딕셔너리를 그렇게 넣어놨음)
  - llm한테 질문을 대신 짜주는 역할을 맡겼고 실제로 유사도가 내려가는 모습을 보여 줌, 기대하지 않았던 의외의 효과가 나타났는데 가령 한번 설명이 끝난 뒤 질문에 주제를 담지 않고 재차 설명을 요청 할 경우 이어지게 됨!! (llm이 question 의값은 변하지 않은채 변한 query룰 받아서 그런것 같음, 이러면 질문의 의도가 변환 시 사라질 수 있을거란 걱정도 해결됨)
  - 인터럽트 기능 자체는 어려운것 같지는 않은데 아직 체크포인터 신경을 안써서 그런것 같음 제대로 들어가면 세션 아이디 같은걸 생각해야 하는데 이때부터 대공사가 될것 같은 느낌 아직 실행은 안돌려봤고 로직 짜기랑 ai 한테 대신 디버깅 정도만 부탁해놨음
  - 클로드 opus 한테 코드를 알려주지 말라고 했더니 내가 구현하고자 하는 기능을 미리 구현을 해놓고 그쪽으로 유도를 시키는 느낌, 분명 더 깔끔한 방법이 생각이 나서 그 부분으로 가고자 했는데 자꾸 말도 안되는 if 를 들먹거리며 자기가 맞다고 우겨서 좀 당황했다
    - case : 새 변수를 선언할 바엔 retry_count를 응용한 방법을 제안했는데 자꾸 웹검색 노드가 무한으로 돌아갈 수 있다고 함, 웹 검색은 진행된 뒤 평가를 들어가지도 않고 바로 마무리인 learn 노드로 들어가기에 반복이 될수가 없는데도 말이다
- 오늘 구현한 부분 : 라우터 이용한 분기 넣기, 인터럽트, 필요한 노드들은 웹 검색 제외하고 다 구현한듯
- 고민인 부분 : 웹 검색 다 api 받아오는것 같아서 뭐가 좋을지 고민해봐야겠음... 이러면 모델 추후 퀜으로 돌려도 막히는 케이스 생길것 같아서

#### 260710
- 오늘은 마감 찍고 집에 가야지
- 오늘의 목표 : 웹 검색 노드 구현(완), 체크 포인터, 모델 변경 (제미나이 -> qwen)
- [rag 검색엔진 비교](https://pleasestudy-alswldi.tistory.com/341)
  - travily : 월 1000회 무로, 내용이 정제된 채로 넘어오며 대표적으로 사용되는 모델, 시간이 오래 걸리는 편
  - duckduckgo : 무료, api키 발급 형식이 아닌 모듈 형식, 다만 직접 정제해야함, 업데이트때마다 에러 발생하는 경우가 잦은듯
  - brave search : 월 2000회 무료이나 무료플랜 사라졌을수도 있다는 얘기가 있음
  - 락 걸리는게 싫음 + 정제 직접 해보고 싶어서 duckduckgo 사용 결정
- duckduckgo : 반환되는 딕셔너리 필드 
  - title(제목), href(페이지 url), body(본문 요약, snippet)
- 체크포인터
  - 인터럽트가 발생하면 인보크가 아예 중단되고 그걸 다시 받으려면 command (resume) 을 해줘야 하는것 같음
  - 메모리 연결이 들어가는 순간 고민거리가 너무 많아질것 같아서 독립적으로 기능 구현 후 메모리와 관련된것들을 마지막에 구현하려고 했는데 인터럽트 기능을 사용하려면 무조건 메모리와 함께 진행해야 할것 같음
  - fastapi 세션 관리... 까지 가면 좀 대형공사같은데