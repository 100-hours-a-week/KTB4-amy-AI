## 260730
- 아니 님들은 env 파일 설정할때 절대경로로 해야하는거 알았음??
- 난 몰랐음!!!!!!!
- 아마도 두가지 에러 발생하는듯
  [진행중]
  - 처음 질문할때 챕터와 관련된 내용이 아니면 에러 뜨는 현상 (폴백을 안해놔서 그런듯)
  
  - 청킹 시 주어가 잘려있으면 누구에 대해 뭐에 대해 설명하는지 모름
    - 내용이 이어져야 하는데 계속 안녕하세요! 우리는 ~에 대해 살펴볼거예요 식 진행 (너무 각 채팅이 끊어지는것처럼 보임)
      [멘토링 내용]
      - 청킹을 여러개 가져오거나 rag 을 하는게 나을듯
      - 가독성은 프롬프트 면에서 해결을 봐야 함
      - rdb 적용하는게 나아보임, 하지만 일단은 벡터 검색으로 해보셈
    - 생각해낸 방안
      - 청킹별로 x 챕터단위로 랭체인에 넣은 뒤 분할하기
        - 랭체인에 넣을때 프롬프트에 가독성을 위한 요소들 + \n\n 기준으로 문단 나눔을 해달라고 요구하기
        - 이후 split 이용해서 설명 분할 text 에 docs 대신 분할된 리스트 넣어주기

  - 현재 db 랑 청킹할때 챕터 캐싱해놓은거 쓰는데 문서 새로 들어올때마다 새롭게 생성할지 db 지우고 다시 할지

  - 데이터 사이즈 어떻게 할건지 (제한을 어떻게 둘건지 생각해보기)

  [완료]
  - 챕터 호출 후 answer 가 null 인 현상
    - 걍 내가 인터럽트 이프에 answer를 추가를 안해놓은거였음
  - n 하면 질의응답으로 빠져야 하는데 answer가 안비워짐
    - ask노드의 return 에 answer값을 추가해놓은 상태였는데 이는 answer가 종료되는 시점에 발생되는거였음
    - 아니 근데 같이 들어간 msg 값은 제대로 변경이 됐는데?
    - 아 해결!! toqa의 경우 answer 또한 인터럽트 시 같이 reply에 담아 던져주었기 때문에 단순 result['answer]가 아니라 v['answer']를 해줬어야 함
    - 근데 이렇게 가면 다른 인터럽트에서 형식 깨질게 분명해서 answer 값 변경 시점을 분기 들어갈때로 변경하여줬음
    - answer 값을 변경하는 clearAnswer 노드를 추가해서 n 누를 시 clearAnswer 노드로 이동하도록 변경 정상 작동 확인 하였음
  - 서브그래프 질의응답도 답변 y/n 으로 받는다고 안내하는 문장 출력하도록
    - 프롬프트 수정 해줬음
    - 얘도 질문 이후 다시 돌아올때 answer 안비워짐
      - 컨디셔널 엣지 수정 해줬음
  - 모델 3.5로 변경하고 env로 모델명 위치 옮기기
  - 모델 토큰 막히면 다른 모델로 전환하기 기능 추가 하고 싶은데
    - 폴백 라이브러리가 있더라!! 이거 쓰면 자동으로 넘어감 함수 통해서 llm 작동할때만 llm에 넣도록 해놨음 굿

## 260731
- 채팅이 매끄럽지 못함
  - 챗봇이지만 답변이 강제되는 상황이 많음
    - ex. 질문하면 y/n 으로만 대답하기 처음 대화 시작할때 챕터 관련이 아니면 오류가 나버림
    - 그러면 이걸 판단하고 알맞은 분기로 보내주면? -> 그토록 넣고 싶었던 에이전트가 아닐까??
- 챕터를 lecture_chain 에 넣을거면 나온 결과물을 db 에 저장하는게 좋을까?
  - 아닌듯 아닌이유
    - 저장할거면 rdb 로 저장해야하는데 이유를 모르겠음
       - 텍스트째로 넣자니 부담이 좀 있고 재활용 하리라는 보장도 없으며 요약해서 저장하면 다시 랭체인을 돌려야 할것이기에 도입이유를 모르겠음
       - 만약 챕터만 저장해놓고 있는다고 해도 어차피 그냥 벡터디비에서 조회하면 되는데? 계층화가 끝난 상황에서는 도입 이유를 모르겠음
       - 심지어 벡터디비는 rag 사용도 가능함!!
- 메모리 관리
  - 문서가 복수로 들어올텐데 이걸 관리를 어떻게 하지
  - 문서 용량 제한 둘건지 얼마나 둘건지
  - 약간 db 적인 고민이 필요한것 같은데 어떻게 해야할지를 모르겠네
  - 채팅 도중에 종료되면 어떻게 시작할건지?
    - 이거 장기 기억으로 차피 대부분 텍스트일테니까 넘길까 했는데 문서 다 저장이 되어있는데 굳이? db 낭비 아녀?
    - 만약 대화가 중간에 끊길것을 대비해 시작할때마다 챕터를 묻는걸로 시작하자!! -> 근데 답변 판단 에이전트 넣을꺼라 굳이 이러지 않아도 될것 같기도 하고
  - 대화 내용은 어떻게 저장할건지?
    - 대화 이어서 하는거는 alex rag 에 있어서 그거 참고하면 좋을듯
  -  현재 db 랑 청킹할때 챕터 캐싱해놓은거 쓰는데 문서 새로 들어올때마다 새롭게 생성할지 db 지우고 다시 할지 (시급)
    - 이건 걍 싹 다 저장해놓고 선택방식으로 가는게 낳지 안흘까 싶긴 함 그 방법 밖에 없는것 같음 노트북엘엠도 그렇게 하고 있기도 하고
- 우선순위
  - 에이전트 구현 - 문서별 구분 어케할지

## 260803
- 남은 구현 순서
  - 의도 분류 마감 (transferQuestion 연결·오타, clarify 엣지, 첫 입력 분류)
  - 사용자 PDF 업로드 + 벡터스토어 구조 
  - 체크포인터 영속화 
  - 프론트엔드 연결

- 의도 분류 마감
  - 그래프 다 짰고 구동도 되는데 오류 남
    - 아마도 현재 애매한 답변이 들어오면 대충 답변 출력하고 학습을 유도하는 방향으로 갔는데 안녕 - 안녕하세요 한번 배워볼까요? - 좋아 에서 좋아를 Y가 아닌 애매모호한 답변으로 판단을 내리고 다시 classify에 들어간게 문제인듯(무한루프가되)
      - build_response 부분에서 인터럽트가 아닐때에는 answer를 result에서만 받아서 resume 값을 받지 못하고 있었음 여기도 result 혹은 입력값을 받도록 수정해주니 501 에러는 발생하지 않음
      - 그러나 위에서 생각한것과 같이 루프 발생 프롬프트를 고쳐주던가 엣지를 처음부터 미확인이 아니라 응아니요로 보내던가...
        - 프롬프트를 손보는 방향으로 갔음 지금도 그래프 충본히 복잡하고 중복되는게 많은것 같아서 또 대답을 어느정도 유도만 하면 되는 부분인것 같아서 대답이 y/n 으로 끝나지 않도록 유도해달라고 했음
        - 그러나 주제를 담아내지 못하고 무슨내용을 공부할까요? 가는식으로 출력되는게 아쉬움
        - 그래서 이것도 대충 프롬프트로 해결 봤음 (너가 모를거 가정해서 유도 질문 출력해라 이런식으로)

- pdf 업로드
  - 1:1 로 할것이냐 1:N 으로 할것이냐
    - 원 계획은 1:1이긴 했으나 이러면 계정 관리도 안되고 아쉬울 것 같음
    - 그렇다고 지금 와서 1:N으로 틀기에는 관리해야 할 부분들이 너무 많음
    - 결론 : 1:1 로 가되 시간이 남으면 1:N으로 확장하자
    - 1. 전처리 함수화
      - 현재 문서를 로드한적 있는지 없는지 디비의 존재를 파악하고 있는데 이를 해주기 위함
      - 전처리 함수화를 통하여 import 단위에서 호출되는 모듈들을 문서 업로드 시점으로 이동해줄것
      - 문제점 : 다른 파일에서 import해서 쓸때가 있어 이 부분 수정이 필요함
      - 함수 안에서 디비 선언할때 어케하지?? 이걸 글로벌화 해줘야 하는데
        - global 붙여주면 전역변수가 된다함... 신기해라 c 랑 다르게 순서 영향을 안받는 대신 순차 진행이 필요하면 이런식으로 가는구나
      - 확실히 디비 관리 쪽 문제가 있긴 한것같엄 일단 문서 교체시 다시 돌아야 하는 부분들 (벡터디비, 청킹, 문서로딩 등)은 전부 함수화 해주었음
      - 캐싱.. 어케하지 지금은 하드코딩으로 위치 고정해놓은 상태
        - 링크를 상대로 만들던가 안쓰던가 생성하는 파일 위치와 기준을 만들어줘야함
        - 근데 굳이 캐시를 써야 하나? 청킹해서 저장을 해주는데 파일을 저장을 안해주는것도 아니고
        - 캐싱 -> 디비 조회로 변경
        - 그러면 디비는 어케 저장함...?
          - 파일명 그대로 저장 / 각 파일마다 id 부여 / 해시
            - 파일명 그대로 : 파일명이 겹치는 경우 대응이 안됨
            - id 부여 : 같은 파일이 재 업로드 되었을때 같은 파일임을 알 수 없음(아이디가 매번 랜덤하게 부여되니까)
            - 위에 단점을 상쇄하는것이 해시이므로 해시를 사용하는것이 타당

## 260804
- fastapi 에서 pdf 파일을 받을때에는 file : uploadfile 을 써주면 사용할 수 있다!
- 디비에 저장하고 나면 파일은 필요 없기에 tempfile 이용하여 캐싱으로 연결, delete=false로 하여 삭제 타이밍을 조절하여주었다. build_vectorstore 트라이 가 끝나면 finally에 있는 파일 삭제가 작동하는 방식
- 기능이 다 돌아가는것을 확인했다! 근데 다 너무 오래걸려서 아쉬움 시간이 진짜 너무 오래걸린다..
  - 다른건 뭐 방법이 없는데 답변 판별 기능 이거,, 과정이 너무 많아서 그런것 같음
    - 예아니요 llm에 넣지 말기 혹은 모델 더 경량화 모델 쓰기 같은거 고민해봐야할듯
    - 라이트 모델로 바꾸니까 빨라졌는데?? 좋아용
- 체크포인터 영속화
  - 나는 short-term / long-term 이 단기기억장치 장기기억장치 인 줄 알았는데 저장하는 단위더라??
    - short-term은 대화 하나 long-term 은 대화 전반적으로 저장하는거 (그래서 롱텀이 특징 같은거 저장했는듯)
      - memorysaver : 메모리에 저장
      - sqlitesaver : 디비에 저장
    - 그렇기에 영속화를 하려면 sqlitesaver 로 변경을 해주어야 한다!
    - sqlitesaver 는 자동으로 저장이 되기 때문에 경로 지정만 해주면 됨
    - 근데 문서는 계속 업로드 해야하는 문제 발생(벡터디비를 전역으로 선언하고 매번 초기화 시키기 때문)
    - 