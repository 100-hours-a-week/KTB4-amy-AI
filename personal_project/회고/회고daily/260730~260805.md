## 260730
- 아니 님들은 env 파일 설정할때 절대경로로 해야하는거 알았음??
- 난 몰랐음!!!!!!!
- 아마도 두가지 에러 발생하는듯
  [진행중]
  - 처음 질문할때 챕터와 관련된 내용이 아니면 에러 뜨는 현상 (폴백을 안해놔서 그런듯)
  
  - 청킹 시 주어가 잘려있으면 누구에 대해 뭐에 대해 설명하는지 모름
    - 내용이 이어져야 하는데 계속 안녕하세요! 우리는 ~에 대해 살펴볼거예요 식 진행 (너무 각 채팅이 끊어지는것처럼 보임)
      [멘토링 내용]
      - 청킹을 여러개 가져오거나 rag 을 하는게 나을듯
      - 가독성은 프롬프트 면에서 해결을 봐야 함
      - rdb 적용하는게 나아보임, 하지만 일단은 벡터 검색으로 해보셈
    - 생각해낸 방안
      - 청킹별로 x 챕터단위로 랭체인에 넣은 뒤 분할하기
        - 랭체인에 넣을때 프롬프트에 가독성을 위한 요소들 + \n\n 기준으로 문단 나눔을 해달라고 요구하기
        - 이후 split 이용해서 설명 분할 text 에 docs 대신 분할된 리스트 넣어주기

  - 현재 db 랑 청킹할때 챕터 캐싱해놓은거 쓰는데 문서 새로 들어올때마다 새롭게 생성할지 db 지우고 다시 할지

  - 데이터 사이즈 어떻게 할건지 (제한을 어떻게 둘건지 생각해보기)

  [완료]
  - 챕터 호출 후 answer 가 null 인 현상
    - 걍 내가 인터럽트 이프에 answer를 추가를 안해놓은거였음
  - n 하면 질의응답으로 빠져야 하는데 answer가 안비워짐
    - ask노드의 return 에 answer값을 추가해놓은 상태였는데 이는 answer가 종료되는 시점에 발생되는거였음
    - 아니 근데 같이 들어간 msg 값은 제대로 변경이 됐는데?
    - 아 해결!! toqa의 경우 answer 또한 인터럽트 시 같이 reply에 담아 던져주었기 때문에 단순 result['answer]가 아니라 v['answer']를 해줬어야 함
    - 근데 이렇게 가면 다른 인터럽트에서 형식 깨질게 분명해서 answer 값 변경 시점을 분기 들어갈때로 변경하여줬음
    - answer 값을 변경하는 clearAnswer 노드를 추가해서 n 누를 시 clearAnswer 노드로 이동하도록 변경 정상 작동 확인 하였음
  - 서브그래프 질의응답도 답변 y/n 으로 받는다고 안내하는 문장 출력하도록
    - 프롬프트 수정 해줬음
    - 얘도 질문 이후 다시 돌아올때 answer 안비워짐
      - 컨디셔널 엣지 수정 해줬음
  - 모델 3.5로 변경하고 env로 모델명 위치 옮기기
  - 모델 토큰 막히면 다른 모델로 전환하기 기능 추가 하고 싶은데
    - 폴백 라이브러리가 있더라!! 이거 쓰면 자동으로 넘어감 함수 통해서 llm 작동할때만 llm에 넣도록 해놨음 굿

## 260731
- 채팅이 매끄럽지 못함
  - 챗봇이지만 답변이 강제되는 상황이 많음
    - ex. 질문하면 y/n 으로만 대답하기 처음 대화 시작할때 챕터 관련이 아니면 오류가 나버림
    - 그러면 이걸 판단하고 알맞은 분기로 보내주면? -> 그토록 넣고 싶었던 에이전트가 아닐까??
- 챕터를 lecture_chain 에 넣을거면 나온 결과물을 db 에 저장하는게 좋을까?
  - 아닌듯 아닌이유
    - 저장할거면 rdb 로 저장해야하는데 이유를 모르겠음
       - 텍스트째로 넣자니 부담이 좀 있고 재활용 하리라는 보장도 없으며 요약해서 저장하면 다시 랭체인을 돌려야 할것이기에 도입이유를 모르겠음
       - 만약 챕터만 저장해놓고 있는다고 해도 어차피 그냥 벡터디비에서 조회하면 되는데? 계층화가 끝난 상황에서는 도입 이유를 모르겠음
       - 심지어 벡터디비는 rag 사용도 가능함!!
- 메모리 관리
  - 문서가 복수로 들어올텐데 이걸 관리를 어떻게 하지
  - 문서 용량 제한 둘건지 얼마나 둘건지
  - 약간 db 적인 고민이 필요한것 같은데 어떻게 해야할지를 모르겠네
  - 채팅 도중에 종료되면 어떻게 시작할건지?
    - 이거 장기 기억으로 차피 대부분 텍스트일테니까 넘길까 했는데 문서 다 저장이 되어있는데 굳이? db 낭비 아녀?
    - 만약 대화가 중간에 끊길것을 대비해 시작할때마다 챕터를 묻는걸로 시작하자!! -> 근데 답변 판단 에이전트 넣을꺼라 굳이 이러지 않아도 될것 같기도 하고
  - 대화 내용은 어떻게 저장할건지?
    - 대화 이어서 하는거는 alex rag 에 있어서 그거 참고하면 좋을듯
  -  현재 db 랑 청킹할때 챕터 캐싱해놓은거 쓰는데 문서 새로 들어올때마다 새롭게 생성할지 db 지우고 다시 할지 (시급)
    - 이건 걍 싹 다 저장해놓고 선택방식으로 가는게 낳지 안흘까 싶긴 함 그 방법 밖에 없는것 같음 노트북엘엠도 그렇게 하고 있기도 하고
- 우선순위
  - 에이전트 구현 - 문서별 구분 어케할지

### 답변 판단 에이전트
