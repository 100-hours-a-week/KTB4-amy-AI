- 이번주 목표
  - fast api 세션 도입 및 human in the loop 마무리하기
  - agent 도입하기
  - 평가기준 마련(ㄹㅇ 더이상 미룰 수 없다)
  - 목차 세분화 하기
  - 개념 설명하기
  - 문서 받기

---

### 260713
- 오늘 목표 : 세션 도입 및 human in the loop 마무리 (완료)
  - 모델 선언시 변수 옆에 | None = None 을 해주면 Is Null 이 된다
  - uuid4는 난수 생성 식별자 스레드 아이디 같이 식별자를 사용해야할때 앞으로도 사용하면 좋을것 같음
  - 그래프를 하다 넘어와서 그런가 fastapi 에서 자꾸 변수를 딕셔너리처럼 사용하려고 들었음 fastapi의 데이터 흐름을 다시 한번 상기시키면 좋을것 같음
  - 현재 루트에 그냥 상태 확인용 get 을 하나 넣어놨는데 auth 라는걸 만든다고 수업중 언급하셔서 그 점 고민해봐도 될듯
  - __interrupt__ 시 result 가 리스트로 받는 이유는 병렬 접근 때문에 리스트로 한다고 함, 형태 변환은 불가 이 상황에는 어차피 들어오는 값이 하나라 그냥 0으로 고정해서 받음 (나중에 병렬되면 수정해야한다는뜻)
  - config 를 따로 선언해줘야 하는 이유는 config가 state와는 다른 기능을 하기 때문에, 딕셔너리의 형태인 이유는 추후 확장성을 위하여 임 참고로 메타데이터 같은것도 이쪽에 들어가는 편

### 260714
- 오늘 목표 : agent 도입 + 목차 생성
  - 굳이 새로운 기능을 넣기 보다는 이미 있는 기능이 tool calling 을 하는게 낫지 않냐는 생각 (넣는다고 해서 답변의 퀄리티가 올라갈것 같지도 않고 지금 그래프도 너무 길어져서 리팩토링 비슷한거 한다고 생각하면 오히려 이쪽이 나을것 같음)
  - 도구화 할만한 노드 : web search, search
  - 이유 : 현재 랭그래프에서 메인 기능 및 분기점이 되어주는것이 이 기능들임 llm에게 어떤 도구를 쓸지 판단의 요소를 주는것이 이 둘이라고 생각했기에 tool 노드는 이 두개로 결정
    - 만들다보니 굳이 싶어짐 이걸 할 바엔 다른 기능을 넣는게 낫지 않을까
  - 도구로 만들만한 기능 이 없는데?? 굳이 니가 알아서 골라서 가 할만한 곳이 없음 ㅜㅜ
- 우선 agent는 후순위로 두고 목차 생성 llm 만들기
  - 목차를 뽑아내는 기준이 어떻게 될것인가
    - 문서를 통째로 넣기에는 토큰이 너무 많이 들어가므로 요약해서 전달해주는것이 필요
    - pymupdf에 폰트의 메타데이터들이 들어가는 점을 이용하여 볼드체와 같이 강조되는 단어들을 찾아서 요약하기로 함
      - 만약 일반적인 텍스트 데이터나 폰트에 변화가 없는 일반 서류 같은 경우는 어쩌나 고민했으나 너무 과한 고민이라는 결론, 애초에 그런류의 서류는 학습이라는 목적과는 맞지 않는 서류라는 결론을 내렸음
      - 엥 당연히 메타데이터에서 넘겨주는줄 알았는데 아니었다
      - pymupdf 에서 pdf의 챕터를 받아오는 기능인 get_toc 사용
        - ppt가 pdf가 된 경우 챕터가 자동 생성되므로 llm에게 판별을 맡기면 될것이며 원래 pdf 문서인데 챕터가 있는 경우라면 챕터가 제대로 들어가 있는것일테니 ppt의 경우처럼 자동생성하다가 잘못 들어온 경우를 걱정하지 않아도 될것이고 없는 경우는 아예 코드를 따로 짜야하기 때문
        - 구현 완료!
      - toc 이 없는 경우에는 글자 타입의 정보를 받아와야 하기 때문에 3중 반복문이라는 복잡한 구조 사용
        - 크기별 글자 수를 받아와서 딕셔너리에 정보 담기 (덮어쓰기의 형태가 아닌 더하기 형태로), 0값 초기화를 어디서 해야하나 고민중 ->  collections.defaultdict 사용
        - 글자 크기가 같은 케이스를 고려하여 폰트 정보도 담아서 세기

### 260715
- 오늘 목표 : 목차 완료(FR-02), 목차 기반 개념 설명 출력 (FR-05)
- 1:N 멘토링 : 바뀐 일정 문서화 및 왜 원래 짰던 일정이 실패했는지 메타적으로 분석하기
- FR-02 끝!! 이 아니었음
  - 코드가 많이 더러워진것 같아 기분이 안좋다.
  - 현재 pymupdf가 주는 폰트 정보를 받아와서 그걸 비교하여 본문을 찾아내고 (max) 그 본문의 정보와 다른것들을 추출해내 목차를 만들어내는 랭체인 도입하였음
  - 공백제거는 strip을 통해 이루어졌음
  - \uf07d 라는 특수기호가 들어가긴 하는데 일종의 강조 표시가 될 수 있지 않을까 싶어서 일단 냅뒀음
  - 서브 챕터 생성을 못하는것 같음
    - 원인 : 텍스트가 쪼개져서 가는게 원인 같음
      - 해결 방법 (지금부터 시도해볼거임) : 문서 정리, 붙이기? 그냥 청킹이랑 비슷할것 같은데..
- FR-05 시작
  - 목차를 기준으로 청킹을 하는게 나을까 하는 고민
    - 다만 이러면 한번에 들어가는 양이 너무 많아질것 같아서 청킹은 유지하는게 나을수 (개념 설명 시 연달아 출력이 가능하다는 전제하에)
    - 아니면 챕터별 출력이 이루어질 경우 llm이 들고있어야 할 메모리 양이 많아질수도 있을것 같음 청킹은 유지하되 이 청킹 데이터와 목차를 연결 짓는 방법을 고민해봐야 할듯
    - 챕터 스키마 양식 (title, page, level)
    - 문서를 넣고 챕터화 후 챕터 목록을 출력한 뒤 어디서부터 시작할까요 묻는식으로 가면 될듯
- FR02&05
  - 솔직히 FR02 전처리 하다보니 청킹이랑 다를게 뭔지 모르겠음, 지금 사용하고 있는 청킹도 동적 청킹이 아닌 fixed 청킹이므로 바꿔주려고 했는데 이 참에 개선하는게 나을듯
  - 챕터가 있고 그 밑에 내용이 들어가는데 부모-자식 청킹 형식이랑 비슷한것 같은데 그러면 여기서 llm 을 넣어서 바로 챕터랑 내용을 구분지으면? 그러면? 청킹도 더 수월해지지 않을까??
    - 근데 그러면 서브 챕터가 생기나? 똑같이 계층 주면 될것 같기는 한데 그러면 검색할때도 메타데이터가 검색이 되나? 프롬프트도 수정이 필요하지 않나? 반환ㄱ값이 완전히 달라질것 같은데ㅔ
    - 부모가 반환이고 자식이 검색이면 자식에 챕터를 부모에ㅔ 내용을 담으면 될것 같은데 이러면 또 질문 시 검색을 제대로 수행해내지 못할것 같음
      -> 절충안 : 부모자식 청크 대신 챕터를 생성해서 메타데이터에 넣자 그러면 시작시 고민됐던 02와05를 어떻게 연결지을 수 있을까가 해결됨
      - 지금 전처리 두번 안하려고 쌩쇼 한건데 왜 전처리가 없지...????????? 전처리 어디갔지???????
  - 청킹 로직
    - Data → 전처리 → 한 번 순회하면서 판별
                    ├─ 제목이면 → 목차(Outline)에 추가
                    └─ 본문이면 → 300자로 청킹 + 방금 지나온 제목을 metadata로 달기 → 임베딩
  - 근데 이러면 그냥 텍스트를 llm에 통째로 넣는건데?? 그러면 굳이 그냥 그냥 이 로직 다시 짜고 동시에 하고 할 필요가 없는데??ㅠ 아니 05에는 도움이 되게ㅅ지.... 근데 서브챕터는? 서브챕터는 누가 짜?
    - 하나 골라야 할덧... 서브 챕터를 포기하기 vs ai 한테 개무리 시키기
    - 근데 나 받은 클로드 ai 얘한테 시키믄 안되나/ 너무 낭비인감 ㅎㅎ 아니 근데 딱 한번 무리하고 마는건데 괜찮지 않아?
    - 드가장 ㅎㅎ 코드 짠거 다 지우지 뭐
    - 머리 안돌아가 오늘은 여기까지...

### 260716
- 오늘 목표 : 문서 전처리 -> 목차 생성 랭체인 -> 서브 챕터 제대로 완성됐나 확인
  - 내용은 그대로 목차는 메타데이터에 넣는 방식
  - 결국 원래 진행하던 fixed chunking으로 갈덧
  - document가 어떻게 작동하는지 감을 잘 못잡는듯 자꾸 이 부분에서 버그 터짐
  - 실행 파일 바꿀때마다 env 다시 해주기