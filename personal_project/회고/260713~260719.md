- 이번주 목표
  - fast api 세션 도입 및 human in the loop 마무리하기
  - agent 도입하기
  - 모델 변경 (gemini -> qwen) 및 파인튜닝
  - 평가기준 마련(ㄹㅇ 더이상 미룰 수 없다)

---

### 260713
- 오늘 목표 : 세션 도입 및 human in the loop 마무리 (완료)
  - 모델 선언시 변수 옆에 | None = None 을 해주면 Is Null 이 된다
  - uuid4는 난수 생성 식별자 스레드 아이디 같이 식별자를 사용해야할때 앞으로도 사용하면 좋을것 같음
  - 그래프를 하다 넘어와서 그런가 fastapi 에서 자꾸 변수를 딕셔너리처럼 사용하려고 들었음 fastapi의 데이터 흐름을 다시 한번 상기시키면 좋을것 같음
  - 현재 루트에 그냥 상태 확인용 get 을 하나 넣어놨는데 auth 라는걸 만든다고 수업중 언급하셔서 그 점 고민해봐도 될듯
  - __interrupt__ 시 result 가 리스트로 받는 이유는 병렬 접근 때문에 리스트로 한다고 함, 형태 변환은 불가 이 상황에는 어차피 들어오는 값이 하나라 그냥 0으로 고정해서 받음 (나중에 병렬되면 수정해야한다는뜻)
  - config 를 따로 선언해줘야 하는 이유는 config가 state와는 다른 기능을 하기 때문에, 딕셔너리의 형태인 이유는 추후 확장성을 위하여 임 참고로 메타데이터 같은것도 이쪽에 들어가는 편